{
  "name": "specteh-pro-backend",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "esbuild server.js --bundle --platform=node --minify --outfile=dist/server.js && cp passenger.htaccess dist/.htaccess"
  },
  "dependencies": {
    "dotenv": "^17.3.1",
    "express": "^4.21.0",
    "http-proxy-middleware": "^3.0.3",
    "mysql2": "^3.20.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "nodemon": "^3.1.0"
  }
}
