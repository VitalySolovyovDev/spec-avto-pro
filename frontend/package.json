{
  "name": "specteh-pro-frontend",
  "private": true,
  "scripts": {
    "dev": "rspack serve --mode development",
    "build": "rspack build --mode production",
    "build:dev": "rspack build --mode development"
  },
  "devDependencies": {
    "@rspack/cli": "^1.7.7",
    "@rspack/core": "^1.7.7",
    "@rspack/dev-server": "^1.2.1",
    "css-loader": "^7.1.4",
    "style-loader": "^4.0.0"
  }
}
